"""NEOFFICE FILE — Owned 100% by Neoservice. Not from upstream OpenConstructionERP.

Neoffice extensions module — Neoservice-specific cross-cutting features that
extend the Neoffice backend without belonging to any regional pack.

Scope: RoomPlan mobile bridge (import + DXF/IFC exports), future mobile-only
endpoints, NORA-integration glue that is too product-specific for upstream.

Regional content stays in `swiss_pack/` (or future `dach_pack/`, `fr_pack/`).
"""
